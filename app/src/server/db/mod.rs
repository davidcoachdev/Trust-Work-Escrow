//! DB — Postgres (twe-postgres) + Mongo (twe-mongo) + moka cache
//! Por ahora placeholder para Task A1, se expande en Task A2/B.

#[cfg(feature = "server")]
pub mod postgres {
    // TODO: sqlx pool para users, verification_tokens
}
