//! Middleware module (stub).
//!
//! Later tasks (T15) add validation, signature auth, rate-limit, CORS and
//! HTTPS enforcement.

/// Placeholder middleware marker. Real middleware arrives in T15.
pub struct Middleware;
