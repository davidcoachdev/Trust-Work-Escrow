//! REST API entrypoint (stub).
//!
//! Tier 0 scaffold (task T1): compiles with no real server. The axum router and
//! handlers are implemented in later tasks (T13–T16) behind the `server` feature.

fn main() {
    println!("trust-escrow-api v3 scaffold: stubs only, no endpoints wired yet.");
}
