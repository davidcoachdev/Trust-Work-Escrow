//! Events module (stub).
//!
//! Later tasks (T9) implement WebSocket/log listeners and decode the events
//! effectively emitted by v3, with a documented fallback when no `#[event]` exists.

/// Placeholder event type. Real decoding arrives in T9.
pub struct EscrowEvent;
