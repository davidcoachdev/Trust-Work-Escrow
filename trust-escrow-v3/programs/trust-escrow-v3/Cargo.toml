[package]
name = "trust-escrow-v3"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "rlib"]

[features]
default = []
idl-build = ["anchor-lang/idl-build"]

[dependencies]
anchor-lang = "0.32.1"
proc-macro2 = { version = "1.0", features = ["span-locations"] }
