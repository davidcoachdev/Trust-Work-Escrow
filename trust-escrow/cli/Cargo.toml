[package]
name = "trust-escrow-cli"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
escrow-core = { path = "../escrow-core" }
clap = { version = "4.5", features = ["derive"] }
anyhow = "1"

[[bin]]
name = "escrow"
path = "src/main.rs"
